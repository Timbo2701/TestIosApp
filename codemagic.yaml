workflows:
  expo-test:
    name: Expo Test Build
    max_build_duration: 30
    instance_type: mac_mini_m2

    scripts:
      - name: Check files
        script: |
          echo "Aktueller Ordner:"
          pwd
          echo "Dateien:"
          ls -la
          echo "package.json Inhalt Anfang:"
          head -20 package.json

      - name: Install dependencies
        script: |
          npm install

      - name: Test Expo export
        script: |
          npx expo export

    artifacts:
      - dist/**
